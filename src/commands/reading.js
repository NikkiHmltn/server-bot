// module.exports = {
//     name: "reading",
//     description: "tracks the number of days since x could read",
//     args: true,
//     execute(message, args) {
//         if (args[0] === "" || !args[0] ) {
//             message.channel.send("Please mention the user you want to reset!")

//         } else {
//             // const user = args[0].id
//             // db.user.findOne({
//                 // where: {name: user}
//             // }).then(user => {
//             //     user.updatedAt = [];
//             // })
//         }
//     }
// }